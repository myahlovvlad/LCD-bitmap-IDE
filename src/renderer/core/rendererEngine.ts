export {
  createFrameBuffer,
  drawBitmap,
  drawLine,
  drawRect,
  drawSpecialElement,
  drawText,
  invertRect,
  packFrameBuffer,
  renderCanvasObjects,
  setPixel,
  unpackBytesToFrameBuffer
} from '../utils/render';

export type { FrameBuffer, RenderOptions } from '../utils/render';
