# ADR-037: Explicit Layout Only

Status: accepted

Phase 4B supports only explicit coordinates and sizes. Table, grid, stack,
constraints, auto-alignment, responsive placement and automatic wrapping are
deferred to Phase 4C.

Unsupported layout modes produce `SCREEN_DSL_UNSUPPORTED_LAYOUT_MODE`.
